# 每个对象中都存在三个数据
# id(标识)、type(类型)、value(值)
a=[1,2,3]
print("修改前",a,id(a))
a[0]=10
# 通过索引修改，内存地址不会变
print("修改后",a,id(a))
# 为变量重新赋值，内存地址会变
a=[4,5,6]
print("修改后2",a,id(a))
b=a
b[0]=10#这个是     改对象
# 加了这个会让改变后的值a会变化
# 改对象会影响所有
b=[7,8,9]#这种就是   改变量
# 这种就是直接改变了b，只有b有变化
print("被b赋值后a",a,id(a))
print("赋值后的b",b,id(b))
# 只有在为变量赋值时才是修改变量，其余都是修改对象